Both programs can be run on the Windows and Linux machines with simple command line command:

  python <program_name>

Moreover, files are executable so syntax like  ./<program_name>   would also work on Linux machines.
